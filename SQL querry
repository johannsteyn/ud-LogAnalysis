#list of all queries used and out puts

Question 1.
select top 3 from articles
where newsdata
order by desc;

Question 2.
select name, sum(views) as total_views from articles
as hits, articles, author order by views desc)
group by name order by total_views desc;
    
  
Question 3.
select time, status
, count(*) percent
from log
where status = '404 NOT FOUND'
group by time, status
having count(*) > 0.02
order by percent desc


 
